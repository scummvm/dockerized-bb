<div class="container">
<div class="row">
<div class="well">
<h2>ScummVM Buildbot nightly builds</h2>
Below are download links for nightly ScummVM builds automatically generated by our buildbot server.
These builds are totally untested, may not be nicely packaged (i.e. may not run without further configuration) and contain debug information so should be used by developers only.
</div>
</div>
<div class="row">
<table class="table table-hover table-striped table-condensed">
<tr>
  <th/>
{% for build in builds -%}
  <th>{{ build.description }}</th>
{% endfor %}
</tr>
{% for platform, packages in platforms.items() -%}
<tr>
  <td>
    <span class="sprite sprite-platforms-{{ platform.icon or 'unknown' }}"></span>
    {{ platform.description }}
  </td>
{% for build in builds -%}
  {% set package = packages[build] -%}
    {% if not package -%}
  <td> <span class="label label-default">N/A</span> </td>
    {% else -%}
      {% set snapshot = package | to_snapshot -%}
      {% if not snapshot -%}
  <td> <span class="label label-danger">Missing</span> </td>
      {% elif not snapshot.revision -%}
  <td> <a class="label label-warning" href="{{snapshot.url}}">Unknown</a> </td>
      {% else -%}
  <td> <a class="label label-success" href="{{snapshot.url}}">{{ snapshot.revision }}</a> </td>
      {% endif -%}
    {% endif -%}
  {% endfor -%}
</tr>
{% endfor -%}
</table>
</div>
</div>
<link href="{{ 'snapshots.css' | static_url }}" rel="stylesheet" />
<link href="https://www.scummvm.org/css/platforms.css" rel="stylesheet" />
